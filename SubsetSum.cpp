#include <bits/stdc++.h>
using namespace std;

int IsSubsetSum(int n, vector<int> arr)
{

    vector<int> visited();
}

int main()
{
    // can't use coin more than once
    //Find weather we can create a number out of ana array
    vector<int> arr = {1, 6, 7, 11};
    int n = 14;
    if (IsSubsetSum(n, arr))
    {
        cout << "Yes!";
    }
    return 0;
}